# CornerGrocer
C++ Inventory Tracker: Analyzes item frequencies using std::map for $O(\log n)$ lookup efficiency. Features data normalization, automated frequency.dat backups, and dynamic CLI histograms via OOP principles. Designed with robust input validation and encapsulated logic to handle complex grocery inventory datasets. 
